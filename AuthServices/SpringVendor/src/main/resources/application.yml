spring:
  application:
    name: SpringVendor
  datasource:
    url: jdbc:mysql://localhost:3306/ECom
    username: root
    password: Navanika@593
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  cloud:
    config:
      enabled: false

server:
  port: 8083

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8061/eureka/