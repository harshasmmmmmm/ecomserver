spring:
  application:
    name: Cartservice
  datasource:
    url: jdbc:mysql://localhost:3306/ECom_Cart
    username: root
    password: Navanika@593
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  cloud:
    config:
      enabled: false

server:
  port: 8085

services:
  security: http://localhost:9999

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8061/eureka/